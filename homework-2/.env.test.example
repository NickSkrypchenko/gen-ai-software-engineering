# Vitest test suite — Neon `test` branch (separate from dev so TRUNCATE doesn't wipe seed data)
# Copy to .env.test and replace DATABASE_URL with your pooled connection string
DATABASE_URL=postgresql://USER:PASS@ep-XXX-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require
NODE_ENV=test
LOG_LEVEL=silent
