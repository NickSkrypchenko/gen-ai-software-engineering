{
  "name": "homework-6-banking-pipeline",
  "version": "1.0.0",
  "description": "AI-Powered Multi-Agent Banking Transaction Pipeline (Homework 6 capstone) — deterministic, file-based Validator → Fraud Detector → Compliance Checker.",
  "author": "Nick Skrypchenko",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/integrator.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "pipeline": "tsx src/integrator.ts",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "mcp": "tsx mcp/server.ts"
  },
  "dependencies": {
    "decimal.js": "^10.4.3",
    "fastmcp": "^1.20.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
